要手工打造一個簡易的消息隊列（MQ）系統以模擬生產者和消費者之間的互動，我們可以創建一個基於 Java 的簡單應用。
這個應用將模擬一個購物場景，當購買一杯咖啡時，庫存會相應減少。

這個例子將包括以下幾個部分：
消息隊列（MessageQueue）：一個簡單的消息隊列，用於儲存消息。
生產者（Producer）：當進行購買操作時，生產者會發送一條消息到隊列，表示有一杯咖啡被購買。(生產咖啡)
消費者（Consumer）：消費者會不斷監聽隊列，一旦有消息，就處理它（比如更新庫存）。(買咖啡)